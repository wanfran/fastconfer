{% extends 'base.html.twig' %}

{% block body %}

    <ul>
        {% for conference in conferences %}
            <li>{{ conference.name }} ----->  <a href="{{ path('showConference', {'slug':conference.slug}) }}">Mostrar mas información</a></li>
        {% else %}
            No hay datos
        {% endfor %}
    </ul>

{% endblock %}
