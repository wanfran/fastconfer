{% extends 'base.html.twig' %}

{% block body %}

    <ul>
            <li>{{ conference.name }}</li>
            <li>{{ conference.description }}</li>

        <a href="{{ path('inscription', {'slug':conference.slug}) }}">Sign up</a>

    </ul>

{% endblock %}